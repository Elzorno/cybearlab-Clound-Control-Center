# Frontend (Planned)

The new frontend will replace `public/index.php` after migration phases complete.

Planned stack:
- Next.js + TypeScript
- Component-based UI aligned to `docs/phase-0/01-ux-and-product-strategy.md`

Current production UI remains:
- `public/index.php`

Prototype UI:
- `frontend/prototype/index.html`
- Open directly in browser and point API base to running backend (default `http://127.0.0.1:8000`).
